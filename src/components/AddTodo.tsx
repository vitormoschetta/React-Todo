import React, { useContext } from 'react';

interface AddTodoForm {
    title: string
}

const AddTodo = () => {   

    return (
        <form>         
        </form>
    );
}

export default AddTodo;